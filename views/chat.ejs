<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Chat Demo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body, html {
      height: 100%;
      margin: 0;
      font-family: Arial, sans-serif;
    }
    .chat-app {
      display: flex;
      height: 100vh;
    }
    .chat-list {
      width: 25%;
      background-color: #f1f1f1;
      overflow-y: auto;
      border-right: 1px solid #ddd;
    }
    .chat-user {
      padding: 15px;
      border-bottom: 1px solid #ddd;
      cursor: pointer;
    }
    .chat-user:hover {
      background-color: #e9e9e9;
    }
    .chat-window {
      width: 75%;
      display: flex;
      flex-direction: column;
    }
    .chat-header {
      padding: 15px;
      border-bottom: 1px solid #ddd;
      background-color: #f8f9fa;
    }
    .chat-messages {
      flex: 1;
      padding: 15px;
      overflow-y: auto;
      background-color: #ffffff;
    }
    .message {
      margin-bottom: 15px;
      max-width: 60%;
      padding: 10px 15px;
      border-radius: 15px;
      clear: both;
    }
    .message.left {
      background-color: #f1f1f1;
      float: left;
    }
    .message.right {
      background-color: #dcf8c6;
      float: right;
      text-align: right;
    }
    .chat-input {
      border-top: 1px solid #ddd;
      padding: 10px;
      background-color: #f8f9fa;
    }
  </style>
</head>
<body>

<div class="chat-app">
  <!-- Left Panel: Chat List -->
  <div class="chat-list">
    <div class="chat-user">
      <strong>Rahul Sharma</strong><br>
      <small>Hey! Are we meeting today?</small>
    </div>
    <div class="chat-user">
      <strong>Anjali Mehta</strong><br>
      <small>Sent you the documents.</small>
    </div>
    <div class="chat-user">
      <strong>Dev Patel</strong><br>
      <small>Let’s catch up soon!</small>
    </div>
  </div>

  <!-- Right Panel: Chat Window -->
  <div class="chat-window">
    <div class="chat-header">
      <h5>Rahul Sharma</h5>
    </div>

    <div class="chat-messages">
      <div class="message left">Hey there!</div>
      <div class="message right">Hi! How are you?</div>
      <div class="message left">Doing great. Meeting today?</div>
      <div class="message right">Yes, let's do 5 PM.</div>
    </div>

    <form action="#" method="POST" class="chat-input d-flex">
      <input type="text" name="message" class="form-control me-2" placeholder="Type a message..." required>
      <button type="submit" class="btn btn-success">Send</button>
    </form>
  </div>
</div>

</body>
</html>
